# Storage Mode Configuration
# Options: 'local' or 'supabase'
# Default: 'local' (uses browser IndexedDB, no account needed)
VITE_STORAGE_MODE=local

# Supabase Configuration (only needed if VITE_STORAGE_MODE=supabase)
# Get these values from your Supabase project settings:
# https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Default API Configuration (Optional)
# These values will be pre-filled in the API configuration form
# Users can still override them in the application
VITE_DEFAULT_API_BASE_URL=https://api.openai.com/v1
VITE_DEFAULT_API_KEY=
VITE_DEFAULT_MODEL=gpt-3.5-turbo
VITE_DEFAULT_PROMPT_TEMPLATE=请把{产品说明}翻译成英文，只接输出翻译后的结果，不要加任何说明和注释。
